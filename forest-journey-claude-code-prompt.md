# Forest Journey — Web 全栈系统开发提示词

> 本提示词基于产品需求文档《知境—森林之旅 意识测评小程序》，将原"微信小程序 + Web管理后台"改造为**纯Web全栈系统**（C端用户端 + B端管理后台）。

---

## 一、项目概述

开发一个名为 **Forest Journey（森林之旅）** 的心理投射测评 Web 系统。用户通过沉浸式的4个场景问答，系统基于映射规则生成服务意识测评报告。C端免费获得"服务者卡片"，付费解锁完整深度报告；B端企业可批量管理测评码、查看团队数据看板、配置岗位模型与人才匹配。

---

## 二、技术栈

请使用以下技术栈（均使用当前最新稳定版）：

| 层级 | 选型 |
|---|---|
| 框架 | Next.js（App Router） |
| 语言 | TypeScript |
| 样式 | Tailwind CSS |
| UI 组件 | shadcn/ui |
| 动画 | Framer Motion |
| 数据库 | PostgreSQL（推荐 Supabase 托管） |
| ORM | Prisma |
| 认证 | NextAuth.js（支持邮箱密码 + Google/GitHub OAuth） |
| 支付 | Stripe |
| 语音识别 | Web Speech API（浏览器原生），兜底用文字输入 |
| 语音合成(TTS) | ElevenLabs API 或浏览器原生 SpeechSynthesis |
| 文件存储 | Supabase Storage 或 AWS S3 |
| NLP/文本分析 | OpenAI API（GPT 最新版做实体抽取与情感分析） |
| 图表可视化 | Recharts 或 Chart.js |
| 部署 | Vercel |

---

## 三、项目结构

采用 Next.js 单体应用，通过路由区分 C端和 B端：

```
forest-journey/
├── app/
│   ├── (public)/          # C端用户端
│   │   ├── page.tsx       # 首页（森林之门）
│   │   ├── meditate/      # 冥想引导页
│   │   ├── assessment/    # 测评流程（4场景）
│   │   ├── result/        # 结果页（卡片+报告）
│   │   ├── profile/       # 个人中心
│   │   └── enterprise/    # 企业测评码入口
│   ├── (admin)/           # B端管理后台
│   │   ├── login/
│   │   ├── dashboard/     # 团队数据看板
│   │   ├── codes/         # 测评码管理
│   │   ├── models/        # 岗位模型配置
│   │   ├── reports/       # 报告管理与导出
│   │   └── members/       # 成员列表
│   └── api/               # API routes
├── components/            # 共享组件
├── lib/                   # 工具函数、映射引擎、NLP等
├── prisma/
└── public/
```

---

## 四、C端（用户端）详细需求

### 4.1 首页

- 深绿色森林主题背景，CSS 动画实现极光/萤火虫粒子缓慢飘浮
- 中央发光森林之门（SVG/CSS 动画）
- 主按钮"开启心灵之旅"，点击进入冥想引导页
- 底部导航：探索（首页）、发现（社区占位）、我的
- 全局轻柔森林白噪音（鸟鸣、风声），进入即播放，用户可开关

### 4.2 冥想引导模块

- 点击"开启心灵之旅"→ 画面变暗 → 中央呼吸光晕动画（缩放循环）
- 自动播放引导音频（TTS 生成或预录制），时长约45秒
- 播放时隐藏所有 UI，仅保留"跳过"按钮
- 播放完毕自动进入第一场景
- 音频加载失败时显示文字稿 + "继续"按钮

### 4.3 核心测评（4个场景）

**通用交互规范：**
- 每道题支持语音输入（Web Speech API）和文字输入切换
- 语音输入后自动转文字填入输入框，可编辑
- 每道题有引导语音（TTS，10-15秒），播放后弹出输入界面
- 所有题目支持"没看清/跳过"

#### 场景一：遇见第一个动物（映射自我认知）

- 引导语后显示主问题"这是什么动物？"
- 输入框 placeholder："例如：一只白色的兔子……"
- 辅助标签栏：有灵性的🦊 / 温厚的🐻 / 警觉的🦌，点击填入输入框
- 追问1："它正在做什么？它的眼神是怎样的？"
- 追问2："它看到你了吗？你们有交流吗？"（可选）
- 数据采集：动物名称 + 描述文本 + 状态标签

#### 场景二：小屋与桌子（映射规则与团队）

- 画面中央一张朴素木桌
- 问题1（桌布）：两个半透明选项卡
  - 左：崭新的，有花纹（配图格子布）
  - 右：旧的，有使用痕迹（配图素色布）
  - 底部"其他"自定义输入
- 问题2（凳子数量）：桌子周围虚线圆圈，点击 +/- 增减（0-8），椅子动画，确认按钮"这样就好"

#### 场景三：墙（映射困难应对）

- 双滑块交互：
  - 滑块1：高度（左"及腰低矮"→ 右"高耸入云"）
  - 滑块2：材质（左"柔软灌木"→ 右"坚硬石砖"）
- 中央的墙根据滑块值实时 CSS 变化
- 确认按钮"这就是那堵墙"
- 问题2：如何过去？选项"轻松翻越/费点劲爬过去/绕路走/找找有没有门" + 自定义输入

#### 场景四：第二个动物（映射客户认知）

- 交互同场景一
- 追加问题"你的第一感觉是什么？"选项：温暖喜悦/想去呵护/平等尊重/有些紧张/好奇观察，用 emoji 展示

### 4.4 测评结果与报告

**生成等待动画：**
- 森林背景中一只鹿跳跃前行，路径花开
- 文案"你的心灵画卷正在展开……"，不超过5秒

**免费卡片（分享钩子）：**
- 根据场景一动物匹配预设插画（可用 AI 生成或预设 SVG 模板库）
- 卡片内容：主称号（如"林间向导·灵狐型服务者"）+ 一句核心解读
- 底部按钮"查看完整心灵图谱 →"
- 分享：生成带二维码的卡片图片（html-to-image 或 Canvas），可下载或复制链接

**完整报告（付费）：**
- Stripe 支付，定价 $1.99（可配置）
- 或邀请3位好友注册免费解锁（生成专属邀请链接）
- 报告长页面可滚动：
  - 你的服务者原型（约500字深度解读）
  - 你的规则感与边界
  - 你与他人的相遇（动物1与动物2关系解读）
  - 你的心灵处方（3条定制化建议）
- 报告底部"我的团队也想探索"企业版入口

### 4.5 个人中心

- 历史测评报告列表（时间倒序），点击回看
- 收藏的卡片陈列
- 设置：音效开关、清理缓存、关于我们、隐私政策、账号注销

### 4.6 企业测评入口

- 首页底部 + 报告末尾两处入口
- 输入企业测评码 → 进入带企业 Logo 的定制测评流程
- 个人报告纳入企业后台数据

---

## 五、B端（管理后台）详细需求

### 5.1 登录与权限

- 企业管理员邮箱密码登录（NextAuth）
- 角色：超级管理员、普通查看者（权限区分）

### 5.2 测评码管理

- 批量生成唯一测评码，设置有效期和最大使用次数
- 列表展示：码、状态（未使用/已使用）、有效期、使用次数
- Excel（CSV）导出测评码列表

### 5.3 团队看板

- 可视化图表展示团队四维分布（共情、规则、应变、角色）
- 支持按部门、时间段筛选
- 成员列表卡片，点击查看完整个人报告

### 5.4 岗位模型与人才匹配

- 预设模型：客户服务专员、销售顾问、技术支持等
- 管理员可调整四维权重创建新模型
- 匹配度计算：选岗位模型 → 系统计算所有测评者匹配度 → 排序展示

### 5.5 报告管理

- 单份/批量导出 PDF 格式个人报告

---

## 六、核心算法：映射引擎

### 6.1 四维定义（0-100分）

- **共情倾向 (Empathy)**
- **规则认同 (Rule)**
- **问题应对韧性 (Resilience)**
- **主导/服务角色 (Role)**

### 6.2 映射规则（初始规则集，设计为可配置）

| 输入 | 维度影响 |
|---|---|
| 动物1：温顺食草类（兔、鹿、羊） | Role→服务，Empathy+ |
| 动物1：猛兽独行类（虎、豹） | Role→专家主导，Empathy- |
| 动物1：群居类（犬、马） | Empathy+，Role→协作 |
| 桌布：新 | Rule+ |
| 桌布：旧 | Rule- |
| 凳子数 0-1 | Role→独立 |
| 凳子数 ≥4 | Role→团队导向 |
| 墙高值 ≥70 | Resilience+ |
| 墙材质 ≥70 | Resilience+ |
| 方式：轻松翻越/绕路 | Resilience适中/变通 |
| 动物2：弱小需呵护 | Empathy++ |
| 动物2：与动物1同类 | Empathy+，平等伙伴 |
| 动物2：有威胁性 | Empathy-，防御心 |

### 6.3 报告生成

- 根据四维得分组合在 ~50 种模板中匹配
- 动态嵌入用户个性化文本片段（动物名、描述等）
- 模板与规则支持管理后台更新
- NLP 文本分析：调用 OpenAI API 抽取动物名称、情感倾向等关键实体

---

## 七、数据库核心表

```
users
  - id, email, name, avatar_url, created_at

assessments
  - id, user_id, enterprise_id?, invite_code?, status(进行中/已完成), started_at, finished_at

answers
  - id, assessment_id, scene_code(S1/S2A/S2B/S3/S4), answer_type(text/choice/slider), content(JSON)

reports
  - id, assessment_id, user_id, role_title, card_image_url, full_report_content(JSON), dimensions_score(JSON), is_paid, created_at

enterprises
  - id, name, logo_url, created_at

invite_codes
  - id, code, enterprise_id, status, expire_date, max_uses, used_count

job_models
  - id, enterprise_id, name, empathy_weight, rule_weight, resilience_weight, role_weight
```

---

## 八、非功能需求

- 首屏加载 < 2秒（Lighthouse 评分 90+）
- 全站 HTTPS，用户数据加密存储
- 测评开始前弹窗隐私同意（GDPR/个人信息保护合规）
- 支持账号注销与数据删除
- 响应式设计：桌面端 + 移动端完美适配
- 按钮热区 ≥ 44×44px，色彩对比度 WCAG AA
- 初期支持 500 并发，架构可水平扩展

---

## 九、开发阶段

### Phase 1：MVP（核心闭环）

1. 项目初始化 + 数据库设计 + 认证系统
2. C端首页 + 冥想引导模块
3. 4个测评场景完整交互（先文字输入，语音输入作为增强）
4. 映射引擎 + 报告生成（免费卡片 + Stripe 付费解锁）
5. 个人中心（历史报告 + 设置）
6. B端登录 + 测评码管理

### Phase 2：完整功能

1. B端团队看板（图表可视化）
2. 岗位模型配置与人才匹配
3. 报告 PDF 导出
4. 企业测评定制流程
5. 分享裂变（邀请解锁）
6. 语音输入/语音引导完整接入

---

## 十、关键交互细节提醒

1. **场景三的墙**需要双滑块实时驱动 CSS 变化，这是视觉亮点，请用 Framer Motion 实现平滑过渡
2. **场景二的凳子**点击 +/- 时要有椅子出现/消失的微动画
3. **冥想引导的呼吸光晕**用 CSS 缩放循环动画，节奏感要舒缓
4. **测评等待动画**鹿跳跃 + 路径花开，用 Lottie 或 Canvas 实现
5. 所有场景切换用淡入淡出过渡，保持沉浸感
6. 全局白噪音用 `<audio>` 标签，页面切换不中断（放在 layout 层）
7. 免费卡片用 Canvas 渲染生成可下载图片
8. 支付流程：点击解锁 → Stripe Checkout → 回调更新 is_paid → 展示完整报告

---

请按以上提示词，从 Phase 1 MVP 开始，逐步实现整个 Forest Journey Web 系统。